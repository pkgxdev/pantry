distributable:
  url: https://gnupg.org/ftp/gcrypt/pinentry/pinentry-{{version}}.tar.bz2
  strip-components: 1

versions:
  url: https://gnupg.org/ftp/gcrypt/pinentry/
  match: /pinentry-(\d+\.\d+(\.\d+)?)\.tar\.bz2/
  strip:
    - /pinentry-/
    - /.tar.bz2/

dependencies:
  gnupg.org/libassuan: '*'
  gnupg.org/libgpg-error: '*'

build:
  dependencies:
    freedesktop.org/pkg-config: '*'
  script: |
    ./configure $ARGS
    make --jobs {{ hw.concurrency }} install
  env:
    ARGS:
      - --prefix="{{prefix}}"
      - --disable-dependency-tracking
      - --disable-silent-rules
      - --disable-pinentry-fltk
      - --disable-pinentry-gnome3
      - --disable-pinentry-gtk2
      - --disable-pinentry-qt
      - --disable-pinentry-qt5
      - --disable-pinentry-tqt
      - --enable-pinentry-tty
    darwin:
      # macOS keeps iconv in the system libiconv (not libc like glibc), and
      # pinentry's configure fails its "working iconv" probe and never links it —
      # so pinentry-curses ends up with undefined _iconv/_iconv_open/_iconv_close.
      # Force -liconv; it also makes the configure probe link cleanly.
      LIBS: -liconv

provides:
  - bin/pinentry
  - bin/pinentry-tty

test: |
  pinentry --version | grep {{version}}
  pinentry-tty --version | grep {{version}}
