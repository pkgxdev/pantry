#!/bin/sh

d=$(dirname "$0")

if [ ! -f "$GNUPGHOME/gpg.conf" ]; then
cat << EOF > $GNUPGHOME/gpg.conf
use-agent
pinentry-mode loopback
agent-program $d/gpg-agent
gpg-connect-agent-program $d/gpg-connect-agent
EOF
fi

$d/gpg-exec "$@"