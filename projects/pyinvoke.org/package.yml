distributable:
  url: https://github.com/pyinvoke/invoke/archive/refs/tags/{{version}}.tar.gz
  strip-components: 1

display-name: pyinvoke.org

versions:
  github: pyinvoke/invoke/tags

dependencies:
  python.org: ^3.13.3

build:
  script:
    - python-venv.sh {{prefix}}/bin/invoke

provides:
  - bin/invoke

test:
  script:
    - invoke --version | grep "^Invoke {{version}}"

