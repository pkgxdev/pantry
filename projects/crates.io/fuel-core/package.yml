# WIP

distributable:
  url: https://github.com/FuelLabs/fuel-core/archive/refs/tags/v{{version}}.tar.gz
  strip-components: 1

provides:
  - bin/fuel-core

versions:
  - 0.14.1

build:
  dependencies:
    rust-lang.org/cargo: '*'
  script:
    cargo xtask build

test:
  fuel-core run --help
