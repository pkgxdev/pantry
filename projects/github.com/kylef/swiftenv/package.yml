distributable:
  url: https://github.com/kylef/swiftenv/archive/refs/tags/{{version}}.tar.gz
  strip-components: 1

versions:
  github: kylef/swiftenv

platforms:
  - darwin

provides:
  - bin/swiftenv

build:
  echo "Nothing to build"

test:
  swiftenv --version | grep {{version}}
