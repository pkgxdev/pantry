distributable:
  url: https://github.com/rupa/z/archive/{{version.tag}}.tar.gz
  strip-components: 1

display-name: z

versions:
  github: rupa/z/tags

build:
  install -D z.sh {{prefix}}/z.sh

test:
  source {{prefix}}/z.sh
