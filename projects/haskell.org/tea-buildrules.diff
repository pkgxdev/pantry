diff -ru a/hadrian/bindist/Makefile b/hadrian/bindist/Makefile
--- a/hadrian/bindist/Makefile	2022-10-26 20:48:46.000000000 -0400
+++ b/hadrian/bindist/Makefile	2022-10-26 20:50:06.000000000 -0400
@@ -206,11 +206,12 @@
 
 PKG_CONFS = $(shell find "$(DESTDIR)$(ActualLibsDir)/package.conf.d" -name '*.conf' | sed "s:   :\0xxx\0:g")
 update_package_db: install_bin install_lib
-	@echo "$(PKG_CONFS)"
-	@echo "Updating the package DB"
-	$(foreach p, $(PKG_CONFS),\
-		$(call patchpackageconf,$(shell echo $(notdir $p) | sed 's/-\([0-9]*[0-9]\.\)*conf//g'),$(shell echo "$p" | sed 's:\0xxx\0:   :g'),$(docdir),$(shell mk/relpath.sh "$(ActualLibsDir)" "$(docdir)")))
-	'$(DESTDIR)$(ActualBinsDir)/$(CrossCompilePrefix)ghc-pkg' --global-package-db "$(DESTDIR)$(ActualLibsDir)/package.conf.d" recache
+	@echo "skipping package db refresh until the end"
+	#@echo "$(PKG_CONFS)"
+	#@echo "Updating the package DB"
+	#$(foreach p, $(PKG_CONFS),\
+	#	$(call patchpackageconf,$(shell echo $(notdir $p) | sed 's/-\([0-9]*[0-9]\.\)*conf//g'),$(shell echo "$p" | sed 's:\0xxx\0:   :g'),$(docdir),$(shell mk/relpath.sh "$(ActualLibsDir)" "$(docdir)")))
+	#'$(DESTDIR)$(ActualBinsDir)/$(CrossCompilePrefix)ghc-pkg' --global-package-db "$(DESTDIR)$(ActualLibsDir)/package.conf.d" recache
 
 install_mingw:
 	@echo "Installing MingGW"
diff -ru a/hadrian/src/Rules/BinaryDist.hs b/hadrian/src/Rules/BinaryDist.hs
--- a/hadrian/src/Rules/BinaryDist.hs	2022-10-26 20:24:02.000000000 -0400
+++ b/hadrian/src/Rules/BinaryDist.hs	2022-10-26 20:39:32.000000000 -0400
@@ -201,7 +201,7 @@
         -- N.B. the ghc-pkg executable may be prefixed with a target triple
         -- (c.f. #20267).
         ghcPkgName <- programName (vanillaContext Stage1 ghcPkg)
-        cmd_ (bindistFilesDir -/- "bin" -/- ghcPkgName) ["recache"]
+        -- cmd_ (bindistFilesDir -/- "bin" -/- ghcPkgName) ["recache"]
 
         -- The settings file must be regenerated by the bindist installation
         -- logic to account for the environment discovered by the bindist
diff -ru a/hadrian/src/Rules/Register.hs b/hadrian/src/Rules/Register.hs
--- a/hadrian/src/Rules/Register.hs	2022-10-28 17:50:19.000000000 -0400
+++ b/hadrian/src/Rules/Register.hs	2022-10-28 20:02:47.000000000 -0400
@@ -95,7 +95,7 @@
         -- a package gets registered but there's not a package.cache file (which
         -- leads to errors in GHC).
         buildWithResources rs $
-            target (Context stage compiler vanilla) (GhcPkg Init stage) [] []
+           target (Context stage compiler vanilla) (GhcPkg Init Stage0) [] []
         writeFileLines stamp []
 
     -- Register a package.
