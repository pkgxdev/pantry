distributable:
  url: https://github.com/skvadrik/re2c/archive/refs/tags/{{ version.raw }}.tar.gz
  strip-components: 1

versions:
  github: skvadrik/re2c/tags

dependencies:
  tea.xyz/gx/cc: c99

build:
  dependencies:
    tea.xyz/gx/cc: c99
    tea.xyz/gx/make: '*'
    gnu.org/automake: '*'
    gnu.org/autoconf: '*'
    gnu.org/libtool: '*'
    freedesktop.org/pkg-config: '*'
  script: |
    ./autogen.sh
    ./configure $ARGS
    make --jobs {{ hw.concurrency }} install
  env:
    ARGS:
      - --prefix="{{prefix}}"

provides:
  - bin/re2c
  - man/man1/re2c.1

test:
  re2c --version