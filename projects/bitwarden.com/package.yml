distributable:
  url: https://registry.npmjs.org/@bitwarden/cli/-/cli-{{version}}.tgz
  strip-components: 1

versions:
  url: https://www.npmjs.com/package/@bitwarden/cli?activeTab=versions
  match: /v\/\d+\.\d+\.\d+/
  strip: /^v\//

dependencies:
  nodejs.org: ^20

build:
  dependencies:
    npmjs.com: '*'
  script:
    - npm i husky
    - git init
    - npm i $ARGS .
    - run: ln -s ../libexec/bin/bw bw
      working-directory: ${{prefix}}/bin
  env:
    ARGS:
      - -ddd
      - --global
      - --build-from-source
      - --prefix={{prefix}}/libexec
      - --install-links
      - --unsafe-perm

provides:
  - bin/bw

test:
  - bw generate --length 10
  - echo "Testing" | bw encode | grep 'VGVzdGluZw'
  - bw --version | grep {{version}}
