{
  "postAttachCommand": "sh <(curl https://pkgx.sh) && pkgx --integrate && clear && echo pkgx integrated && exec bash -c 'dev && echo dev environment loaded || echo error loading dev environment; exec $SHELL'"
}
